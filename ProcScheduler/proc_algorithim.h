#pragma once
#include "csv_load.h"
class proc_algorithim
{
public:
	static void roundRobin(csv_load);
	static void HRRN(csv_load);
	static void roundRobin(csv_load);
	static void shortestProcNext(csv_load);
};

